node_modules
.env
# Variantes do .env — backup, por-ambiente, por-maquina. Todas tem segredo
# dentro, e `.env` sozinho casa so o nome exato: um `.env.bak` entraria no
# commit sem ninguem notar. O .env.example e o unico versionado.
.env.*
!.env.example
dist
dist-ssr
dist-app
*.local

# Editor
.vscode/*
!.vscode/extensions.json
.idea

# Android (Capacitor): o projeto e versionado, o que o Gradle produz nao
android/app/build
android/build
android/.gradle
android/local.properties
android/app/src/main/assets/public
android/app/src/main/assets/capacitor.config.json
android/app/src/main/res/xml/config.xml
android/capacitor-cordova-android-plugins
